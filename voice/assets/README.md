Reference voice clips used as `ref_wav` in [profiles.yaml](../profiles.yaml).

Drop short (a few seconds) WAV clips here:

- `male_reference.wav` — a male speaker sample
- `female_reference.wav` — a female speaker sample

These are sent to lvoice as `prompt_audio` for zero-shot voice cloning.
